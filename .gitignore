.DS_Store
node_modules
# GS sidecar binaries are tracked via `git add -f` — this rule prevents
# accidental addition of intermediate build outputs. To update a binary, use:
# cp <new-binary> src-tauri/binaries/gs-<target-triple> && git add -f src-tauri/binaries/gs-<target-triple>
src-tauri/binaries/gs-*
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.superpowers/
CLAUDE.md
.mcp.json
docs/superpowers/
.impeccable.md
.vscode/
